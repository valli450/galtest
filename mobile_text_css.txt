/* БЛОК ИНФОРМАЦИИ (справа на десктопе) — на телефоне сверху */
.info-right{
  right:5%; left:5%;
  top:50px;
  bottom:auto;
  max-width:none;
  text-align:center;
  transform:none !important;
}
.info-right .eyebrow{ font-size:9px; letter-spacing:.3em; }
.info-right .title{ font-size:18px; }
.info-right .desc{ font-size:11px; line-height:1.5; }

/* БЛОК ЦИТАТЫ (слева на десктопе) — на телефоне снизу */
.letter-left{
  left:5%; right:5%;
  top:auto;
  bottom:50px;
  max-width:none;
  text-align:center;
  transform:none !important;
}
.letter-left .quote{ font-size:11px; line-height:1.5; }
.letter-left .ref{ font-size:9px; }
